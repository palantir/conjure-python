type: fix
fix:
  description: Validate identifiers in the input Conjure IR before generating code;
    identifiers that violate Conjure's grammar (for example enum values or type names)
    are now rejected rather than emitted verbatim into the generated Python.
  links:
  - https://github.com/palantir/conjure-python/pull/1323
